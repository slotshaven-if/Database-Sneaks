# Opgaver

 til 0-elements-opgaver.sql

## Opgave: SELECT

Skriv en forespørgsel, der viser element, symbol og atomic_no for alle grundstoffer.

Opdater prisen på 'Vans Old Skool' til 799.00 kr

Tjek at prisen er blevet opdateret ved at køre en SELECT-forespørgsel.
Skriv din forespørgsel herunder:

```sql
-- Skriv din forespørgsel herunder.
SELECT   ..
```

## Opgave: SELECT

Skriv en forespørgsel, der viser element, symbol og atomic_no for alle grundstoffer.

```sql
-- Skriv en forespørgsel, der viser element, symbol og atomic_no for alle grundstoffer.
-- Skriv din forespørgsel herunder.
SELECT   ..
```

## Opgave: SELECT 1

Skriv en forespørgsel, der viser element og phase for alle grundstoffer, hvor phase = 'solid'.

```sql
-- Skriv en forespørgsel, der viser element og phase for alle grundstoffer, hvor phase = 'solid'.
-- Skriv din forespørgsel herunder.
SELECT   ..
```

## Opgave: SELECT 2

Skriv en forespørgsel, der viser element, atomic_mass og density for alle grundstoffer, hvor density > 5.

```sql
-- Skriv en forespørgsel, der viser element, atomic_mass og density for alle grundstoffer, hvor density > 5.
-- Skriv din forespørgsel herunder.
SELECT   ..
```

## Opgave: SELECT 3

Skriv en forespørgsel, der viser element og "year" for alle grundstoffer, som er opdaget før 1800, sorteret efter "year" stigende.

```sql
-- Skriv en forespørgsel, der viser element og "year" for alle grundstoffer, som er opdaget før 1800, sorteret efter "year" stigende.
-- Skriv din forespørgsel herunder.
SELECT   ..
```

## Opgave: SELECT 4

Skriv en forespørgsel, der viser de 10 første grundstoffer med element og atomic_no, sorteret efter atomic_no.

```sql
-- Skriv en forespørgsel, der viser de 10 første grundstoffer med element og atomic_no, sorteret efter atomic_no.
-- Skriv din forespørgsel herunder.
SELECT   ..
```
